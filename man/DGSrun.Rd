% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapperFuns.R
\name{DGSrun}
\alias{DGSrun}
\title{Run the Discrete Gap Statistic workflow and save plots}
\usage{
DGSrun(
  x,
  catVals,
  dataClass,
  clusterFUN = "pam",
  B = 100,
  K.max = 7,
  value.range = "DS",
  distName = "hamming",
  useLog = TRUE,
  title = NULL,
  outDir = "./"
)
}
\arguments{
\item{x}{A matrix of categorical observations. Must be a matrix (enforced).}

\item{catVals}{Vector of possible category values for the variables in \code{x}.}

\item{dataClass}{Data class indicator passed to \code{\link{clusGapDiscr}}.}

\item{clusterFUN}{Character name of a clustering algorithm (default \code{"pam"}).}

\item{B}{Integer number of bootstrap samples for the gap statistic.}

\item{K.max}{Maximum number of clusters considered.}

\item{value.range}{Character string passed to \code{\link{clusGapDiscr}}.}

\item{distName}{Character name of the distance metric (default \code{"hamming"}).}

\item{useLog}{Logical; passed to \code{\link{clusGapDiscr}}.}

\item{title}{Optional title prefix used in plot titles and output filenames.}

\item{outDir}{Output directory where PNG files will be written.}
}
\value{
The object returned by \code{\link{clusGapDiscr}}.
}
\description{
High-level wrapper that (i) saves a distance heatmap, (ii) runs
\code{\link{clusGapDiscr}}, (iii) saves a Gap Statistic plot (with the chosen
\code{K} marked), (iv) saves the resulting categorical heatmap, and (v) attempts
to save an MDS plot colored by the chosen clusters.
}
\details{
This function includes a device "safety rail" that closes any graphics devices
opened during its execution (useful if downstream plotting code forgets to call
\code{dev.off()}).
}
